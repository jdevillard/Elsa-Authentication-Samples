{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Backend": {
    "Url": "https://localhost:7022/elsa/api"
  },
  "Auth0": {
    "Domain": "--AUTH0 DOMAIN--",
    "ClientId": "--AUTH0 ClientID--",
    "ClientSecret": "--AUTH0 Secret If you need Auth0 access token -> need configuration of server side to validate against Auth0 --",
    "Audience": "--AUTH0 Audience if you want to use access token -> need configuration of server side to validate against Auth0 --"

  }
}
